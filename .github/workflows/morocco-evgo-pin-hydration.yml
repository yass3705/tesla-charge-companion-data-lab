name: Morocco EVGO pin hydration

on:
  workflow_dispatch:
  push:
    paths:
      - 'scripts/morocco_evgo_pin_hydration_probe.py'
      - 'scripts/morocco_evgo_normalize.py'
      - '.github/workflows/morocco-evgo-pin-hydration.yml'

permissions:
  contents: write

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Run sanitized read-only probe
        run: python3 scripts/morocco_evgo_pin_hydration_probe.py
      - name: Stage sanitized source report
        run: |
          mkdir -p reports/morocco/evgo
          cp artifacts/morocco-evgo-pin-hydration/summary.json reports/morocco/evgo/latest-pin-hydration.json
      - name: Normalize native station model
        run: python3 scripts/morocco_evgo_normalize.py
      - name: Publish sanitized and normalized reports
        env:
          RUN_ID: ${{ github.run_id }}
        run: |
          cp reports/morocco/evgo/latest-pin-hydration.json /tmp/latest-pin-hydration.json
          cp reports/morocco/evgo/latest-normalized-stations.json /tmp/latest-normalized-stations.json
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          # A rerun can start from an older workflow SHA while main keeps moving.
          # Re-anchor on current main before restoring the freshly generated,
          # sanitized reports so publication remains a fast-forward update.
          git fetch origin main
          git reset --hard origin/main
          mkdir -p reports/morocco/evgo
          cp /tmp/latest-pin-hydration.json reports/morocco/evgo/latest-pin-hydration.json
          cp /tmp/latest-normalized-stations.json reports/morocco/evgo/latest-normalized-stations.json
          git add reports/morocco/evgo/latest-pin-hydration.json reports/morocco/evgo/latest-normalized-stations.json
          if git diff --cached --quiet; then
            echo 'No report changes'
            exit 0
          fi
          git commit -m "data(morocco): update EVGO native V9 model [run ${RUN_ID}]"
          git push origin HEAD:main
