name: Alizé Toulouse official tariffs

on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - ".github/workflows/alize-toulouse-official-tariffs.yml"
      - "config/operator_manifests/alize_toulouse.json"
      - "scripts/operator_manifest_runner.py"
      - "scripts/public_safety_check.py"

permissions:
  contents: write

concurrency:
  group: alize-toulouse-official-tariffs
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Public-safety scan
        run: python scripts/public_safety_check.py .

      - name: Extract and validate official evidence
        id: extract
        shell: bash
        run: |
          set -o pipefail
          mkdir -p out/alize_toulouse
          python -m py_compile scripts/operator_manifest_runner.py
          python scripts/operator_manifest_runner.py --operator alize_toulouse --out out/alize_toulouse 2> >(tee out/alize_toulouse/error.log >&2)
          cat out/alize_toulouse/SUMMARY.md >> "$GITHUB_STEP_SUMMARY"

      - name: Publish sanitized diagnostic on failure
        if: failure() && steps.extract.outcome == 'failure'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p reports/alize_toulouse
          python - <<'PY'
          import json,re
          from pathlib import Path
          p=Path('out/alize_toulouse/error.log')
          text=p.read_text(errors='replace') if p.exists() else 'extractor failed without stderr capture'
          text=re.sub(r'(?i)(authorization|cookie|token|secret|password)\s*[:=]\s*\S+',r'\1=[REDACTED]',text)
          Path('reports/alize_toulouse/latest_diagnostic.json').write_text(json.dumps({
              'dataset':'alize-toulouse-individual-diagnostic',
              'operator':'Alizé Toulouse',
              'sanitized':True,
              'runId':'${{ github.run_id }}',
              'errorTail':[x for x in text.splitlines() if x.strip()][-60:]
          },ensure_ascii=False,indent=2)+'\n')
          PY
          python scripts/public_safety_check.py .
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/alize_toulouse/latest_diagnostic.json
          if ! git diff --cached --quiet; then
            git commit -m "diag(alize-toulouse): publish operator diagnostic [run ${GITHUB_RUN_ID}]"
            for attempt in 1 2 3; do
              git pull --rebase origin main
              if git push origin HEAD:main; then exit 0; fi
              sleep $((attempt * 2))
            done
            exit 1
          fi

      - name: Validate output envelope
        if: success()
        shell: bash
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          m=json.loads(Path('config/operator_manifests/alize_toulouse.json').read_text())
          p=json.loads((Path('out/alize_toulouse')/m['outputFile']).read_text())
          assert p['dataset'] == m['dataset']
          assert p['operator'] == 'Alizé Toulouse'
          assert p['country'] == 'FR'
          assert p['publicationStatus'] == 'candidate_validated_source'
          assert p['sourceEvidence']['officialOnly'] is True
          assert len(p['sourceEvidence']['relevantTariffFingerprintSha256']) == 64
          assert p['operatorDirect']['alizeLibertePublic']['normalBelow22Kw']['eurPerKwh'] == 0.4
          assert p['operatorDirect']['preferredSubscription']['monthlyFeeEur'] == 4.0
          assert p['operatorDirect']['badgePriceEur'] == 12.0
          assert p['twoWheelers']['eurPerHour'] == 1.5
          assert p['twoWheelers']['capEur'] == 4.5
          assert p['roaming']['mustRemainSeparateFromAlizeToulouseDirectTariff'] is True
          print('Validated Alizé Toulouse output envelope')
          PY

      - name: Publish validated rule set only when model changes
        id: publish
        if: success()
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json,shutil
          from pathlib import Path
          m=json.loads(Path('config/operator_manifests/alize_toulouse.json').read_text())
          src=Path('out/alize_toulouse')/m['outputFile']
          dst=Path('data/operator_direct')/m['outputFile']
          rep=Path('reports/alize_toulouse/latest_summary.md')
          new=json.loads(src.read_text())
          def sig(d):
              return {
                  'dataset':d.get('dataset'),
                  'operator':d.get('operator'),
                  'facts':{k:v for k,v in d.items() if k not in {'generatedAt','sourceEvidence'}},
                  'fingerprint':(d.get('sourceEvidence') or {}).get('relevantTariffFingerprintSha256'),
              }
          changed=not dst.exists() or sig(json.loads(dst.read_text())) != sig(new)
          Path('changed.flag').write_text('1' if changed else '0')
          if changed:
              dst.parent.mkdir(parents=True,exist_ok=True)
              rep.parent.mkdir(parents=True,exist_ok=True)
              shutil.copy2(src,dst)
              shutil.copy2(Path('out/alize_toulouse/SUMMARY.md'),rep)
          PY
          echo "changed=$(cat changed.flag)" >> "$GITHUB_OUTPUT"
          python scripts/public_safety_check.py .

      - name: Commit validated Alizé Toulouse rule set
        if: success() && steps.publish.outputs.changed == '1'
        shell: bash
        run: |
          set -euo pipefail
          output=$(python - <<'PY'
          import json
          from pathlib import Path
          print(json.loads(Path('config/operator_manifests/alize_toulouse.json').read_text())['outputFile'])
          PY
          )
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "data/operator_direct/$output" reports/alize_toulouse/latest_summary.md
          git commit -m "data(alize-toulouse): publish official tariff rules [run ${GITHUB_RUN_ID}]"
          for attempt in 1 2 3; do
            git pull --rebase origin main
            if git push origin HEAD:main; then exit 0; fi
            sleep $((attempt * 2))
          done
          exit 1
